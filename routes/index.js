
const express = require('express');
const router = express.Router();
const db = require('firebase-admin').firestore();

// TODO Define HTTP routes for this module


module.exports = router;
